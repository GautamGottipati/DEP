import { DeleteUserPipe } from './delete-user.pipe';

describe('DeleteUserPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteUserPipe();
    expect(pipe).toBeTruthy();
  });
});
