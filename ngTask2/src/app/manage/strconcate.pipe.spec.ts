import { StrconcatePipe } from './strconcate.pipe';

describe('StrconcatePipe', () => {
  it('create an instance', () => {
    const pipe = new StrconcatePipe();
    expect(pipe).toBeTruthy();
  });
});
